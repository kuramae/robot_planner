package planner.strips;

import knowledge.State;
import planner.Plan;
import planner.PlanExecutor;

public class StripsPlanExecutor implements PlanExecutor {
    State executePlan(Plan plan) {
        return null;
    }
}
